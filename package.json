{
  "name": "linter-monorepo",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "publish:all": "lerna publish from-package --yes"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "engines": {
    "node": ">= 18.0.0"
  }
}
